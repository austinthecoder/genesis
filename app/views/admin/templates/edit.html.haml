.section
  %h2
    = link_to "Theme", admin_theme_path
    > Templates >
    = @tpl.name

  %p= link_to "template data", admin_template_fields_path(@tpl)

  = render 'form'

  = content_for :sidebar do
    = render 'list'